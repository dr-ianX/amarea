Arrastra aquí fotos, clips cortos, loops y vídeos.
Soportados: .jpg .jpeg .png .gif .webp .avif .svg .mp4 .webm .mov .ogv .mkv .avi

Si la multimedia completa está en otra carpeta (p. ej. `Amarea/Relay`),
corre `npm run sync-build` para copiar `fotos` y `videos` y regenerar `multimedia.json`.
